rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow writes to the complaints folder for attachments
    match /complaints/{issueId}/{allPaths=**} {
      allow write: if true;
    }
  }
}
