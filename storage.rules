rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow writes to the issues folder for anyone
    match /issues/{issueId}/{fileName} {
      allow write: if true;
    }

    // Allow public read access to all files
    match /{allPaths=**} {
      allow read: if true;
    }
  }
}
